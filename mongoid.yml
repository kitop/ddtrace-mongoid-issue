test:
  clients:
    default:
      database: "example"
      hosts:
        - mongo1
        - mongo2
        - mongo3
      options:
        replica_set: rs0

  options:
    log_level: :info
    app_name: example

